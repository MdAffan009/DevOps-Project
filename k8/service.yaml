apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    app: webapp

spec:
  type: ClusterIP

  selector:
    app: webapp
  
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000